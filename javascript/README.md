# JavaScript

## Env Setup

- Install Node.js

- To add a package.json file run:

```powershell
npm init
```

- To add a eslint.json run:

```powershell
npx eslint --init
```

- To restore packages in packages.json run:

```powershell
npm install restore
```
